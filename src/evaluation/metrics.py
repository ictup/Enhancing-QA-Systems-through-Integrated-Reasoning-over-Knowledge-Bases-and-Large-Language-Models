from typing import List, Tuple
from sklearn.metrics.pairwise import cosine_similarity
import numpy as np

def bertscore_placeholder(preds: List[str], refs: List[str]) -> Tuple[float, float, float]:
    # Placeholder; use actual BERTScore in notebooks or a proper wrapper
    return 0.0, 0.0, 0.0
